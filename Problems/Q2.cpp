#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, key;
    cin >> n;
    cin >> key;
    int A[n];
    for (int i = 0; i < n; i++) {
        cin >> A[i
        
        ];
    }
    while (n--) {
        if (A[n] == key) {
            cout << n;
            break;
        }
    }
    return 0;
}